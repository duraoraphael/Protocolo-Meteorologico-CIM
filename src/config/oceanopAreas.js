// Fill only with local identifiers confirmed by Oceanop.
// Keys must match src/config/cities.js. No mapping is inferred from city names.
// Shape: cidade_chave: { local: 'identificador-confirmado' }
module.exports = {};
